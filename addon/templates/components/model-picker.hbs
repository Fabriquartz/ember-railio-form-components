{{auto-complete content=content
                value=value
                onQueryChange=(action "lookupModel")
                optionLabelPath=optionLabelPath
                groupLabelPath=groupLabelPath
                prompt=prompt
                name=name
                updated=updated
                disabled=disabled}}
