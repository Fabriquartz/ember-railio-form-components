{{auto-complete content=content
                multiSelect=multiSelect
                value=value
                onQueryChange=(perform lookupModel)
                optionLabelPath=optionLabelPath
                groupLabelPath=groupLabelPath
                prompt=prompt
                name=name
                updated=updated
                disabled=disabled}}
