{{auto-complete content=groupedContent
                multiSelect=multiSelect
                value=value
                onQueryChange=(unless preload (perform lookupModel))
                optionLabelPath=optionLabelPath
                groupLabelPath=groupLabelPath
                prompt=prompt
                name=name
                updated=updated
                disabled=disabled}}
